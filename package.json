{
  "name": "@kosovopay/server-sdk",
  "version": "0.3.0",
  "description": "Type-safe, runtime-agnostic server SDK for the KosovoPay payment orchestrator. Runs on Node, Bun, Deno, and Cloudflare Workers — zero dependencies.",
  "type": "module",
  "license": "MIT",
  "author": "shkumbinhasani",
  "homepage": "https://pay.kosovo.sh/docs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kosovopay/js-server-sdk.git"
  },
  "bugs": {
    "url": "https://github.com/kosovopay/js-server-sdk/issues"
  },
  "keywords": [
    "kosovopay",
    "payments",
    "payment-orchestrator",
    "stripe-like",
    "cloudflare-workers",
    "bun",
    "webhooks",
    "typescript"
  ],
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && bun run scripts/fix-dts.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check",
    "format": "biome check --write",
    "test": "bun test",
    "smoke": "bun run build && bun scripts/smoke.mjs",
    "ci": "bun run typecheck && bun run lint && bun test",
    "prepack": "bun run build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@changesets/cli": "^2.31.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
